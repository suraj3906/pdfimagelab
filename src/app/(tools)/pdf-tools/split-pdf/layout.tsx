import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Split PDF Online - Extract Pages from PDF Free",
  description: "Split PDF files into individual pages quickly and securely. Extract pages from your PDF documents directly in your browser.",
  keywords: ["split pdf", "extract pdf pages", "separate pdf", "pdf divider"],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
