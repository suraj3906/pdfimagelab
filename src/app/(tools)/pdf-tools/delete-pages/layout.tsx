import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Delete Pages from PDF - Remove PDF Pages Online Free",
  description: "Remove specific pages from your PDF documents securely. 100% free and processed locally in your browser.",
  keywords: ["delete pdf pages", "remove pdf pages", "extract pdf", "edit pdf"],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
