import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Compress PDF Online - Reduce PDF File Size Free",
  description: "Compress PDF files online for free. Reduce PDF file size quickly without losing quality. Secure, fast, and works directly in your browser.",
  keywords: ["compress pdf", "reduce pdf size", "shrink pdf", "pdf optimizer"],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
