import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Compress Image Online - Reduce JPG, PNG, WEBP Size",
  description: "Compress images online for free. Reduce file size of JPG, PNG, and WEBP images while maintaining high quality. 100% secure client-side processing.",
  keywords: ["compress image", "reduce image size", "image optimizer", "shrink image"],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
